package backend.academy.fractal.transformation;

import backend.academy.fractal.grid.Point;
import backend.academy.fractal.transformation.impl.SinusoidalTransformation;

public enum Transormation {
    /*//TODO add
    SINUSOIDAL(new SinusoidalTransformation()),
    SPHERICAL,
    POLAR,
    HEART,
    LINEAR,
    DISK;

    private final TransformationFunction function;
    Transormation(TransformationFunction function) {
        this.function = function;
    }

    public Point transform(Point point) {
        return this.function.apply(point);
    }*/
}
